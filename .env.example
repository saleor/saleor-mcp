# Public URL overrides are useful for local tunnels. Leave blank on Vercel.
APP_IFRAME_BASE_URL=
APP_API_BASE_URL=

# Local development uses the template's FileAPL by default.
# Production must choose a persistent APL. The included adapter supports dynamodb.
APL_PROVIDER=file
APL_DYNAMODB_TABLE=
AWS_REGION=

# Optional full-match regex limiting which Saleor API URLs may install the app.
ALLOWED_DOMAIN_PATTERN=

# At least 32 random bytes. Used only to sign MCP-facing installation credentials.
MCP_CREDENTIAL_SECRET=
